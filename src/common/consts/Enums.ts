export enum FollowType {
    User = 1,
    TOPIC = 2,
}

export enum WsMsgType {
    NoticeNumIncr = 1,
    PrivateChatMsg = 2,
    PrivateChatRecallMsg = 3,
    BroadcastChatMsg = 4,
}

export enum ChatMsgType{
    PrivateChatMsg=1,
    BroadcastChatMsg=2,
}